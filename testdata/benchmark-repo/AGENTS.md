# Mercury contributor instructions

- Keep user-facing configuration behavior documented in `docs/configuration.md`.
- Prefer focused changes and deterministic tests.
- Do not add compatibility aliases for renamed public configuration keys.
- Repository investigation answers must cite workspace-relative files and exact line ranges.
