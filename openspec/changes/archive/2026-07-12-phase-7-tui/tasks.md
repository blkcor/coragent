## 1. Freeze the additive public contracts

- [x] 1.1 Add versioned observed-event envelopes and typed payloads for the exact schema-v1 kinds `run_started`, `status_changed`, assistant lifecycle/deltas, tool lifecycle, permission, usage, omission, hook, subagent lifecycle, warning, error, and `run_finished` under `pkg/agent`, including stable run, agent, call, request, preview-revision, origin, sequence, and timestamp fields.
- [x] 1.2 Add typed context-usage, omission, action-preview, sandbox-grant, capability, and subagent-outcome value types with explicit unknown states and deep-copy behavior; cleanly reject an unsupported schema version or unknown kind declared under the closed v1 schema.
- [x] 1.3 Add an immutable, secret-free `Session.Describe` result that reports model identity, provider feature support, working directory, permission ownership plus typed mode when the standard engine owns it, sandbox posture, context-window knowledge, and truthful capability inventory.
- [x] 1.4 Promote permission mode to a public typed enum with between-run getter and setter behavior, retain the existing string setter and documented valid behavior as a wrapper, make its previously unspecified mid-run use return the same stable error, and cover both setters offline.
- [x] 1.5 Add the opt-in `Session.RunObserved` API while preserving the signatures, source compatibility, and payload behavior of `Session.Run`, `Provider`, `RunEvent`, `SessionConfig`, `NewSession`, and `NewSessionWithError`.
- [x] 1.6 Add compile-time and table-driven compatibility fixtures proving an unmodified legacy provider and SDK client still compile and receive the same legacy event sequence and terminal result.

## 2. Build one authoritative rich runtime stream

- [x] 2.1 Introduce one internal rich event envelope in `internal/core` and adapt `internal/sessionrun` plus `internal/loop` so every run computes lifecycle, correlation, origin, and terminal outcome once.
- [x] 2.2 Implement deterministic projections from that one internal stream to both the observed API and the unchanged legacy `RunEvent` API, dropping rich-only facts instead of encoding them into strings or legacy fields; adapt caller-owned legacy permission events to a limited `legacy_one_shot` observed prompt without dropping their reply path.
- [x] 2.3 Guarantee monotonic sequence numbers, stable IDs, one terminal envelope, channel closure after terminal, and no events after terminal across success, provider error, hook block, cancellation, and step-limit paths; make legacy and rich provider-channel receives select on context so cancellation cannot wait forever for channel closure.
- [x] 2.4 Define and implement the optional rich-provider interface plus a legacy-provider adapter without adding a method to the existing required `Provider` interface.
- [x] 2.5 Extend the OpenAI-compatible provider adapter to report designated display-safe reasoning summaries, structured usage, optional context-window metadata, and distinct stop, tool, length, filter, and failure termination reasons when supported.
- [x] 2.6 Ensure provider adapters never request or forward hidden chain-of-thought, never append reasoning summaries to `Conversation`, and omit summary content when no display-safe field is available.
- [x] 2.7 Add scripted provider and loop tests for rich ordering, legacy projection, unknown optional fields, malformed usage, length cutoff, content filtering, cancellation, and exactly one terminal event.

## 3. Make context and omission reporting truthful

- [x] 3.1 Extend `internal/context` to estimate the effective assembled request before each provider round, including durable conversation, transient injected context, tool schemas, and available request framing.
- [x] 3.2 Emit typed usage snapshots at stable round boundaries with source, round, measured time, used tokens, optional window, optional remaining capacity, and no fabricated percentage when the window is unknown.
- [x] 3.3 Let valid provider usage supersede that round's estimate while keeping estimated and provider-sourced values distinguishable and preserving the existing independent budget warning behavior.
- [x] 3.4 Add the v1 omission taxonomy `output_budget`, `preview_budget`, `provider_length`, `content_filter`, and `redacted`, reserving `context_compaction` without claiming that Phase 7 compacts history.
- [x] 3.5 Keep frontend folding out of omission events and attach truthful retained/original sizes, scope, correlation, recoverability, and typed `unknown`/`unavailable`/`new_user_turn` continuation mode only for irreversible loss known to the harness; define new-turn continuation as an editable follow-up, never provider-level resume or auto-send.
- [x] 3.6 Add offline estimator, precedence, unknown-window, malformed-provider-usage, warning-compatibility, omission-ordering, and no-compaction tests.

## 4. Prepare and approve exact candidate actions

- [x] 4.1 Add an optional side-effect-free prepared-action handler contract in `internal/core` that returns effective arguments, a structured bounded preview, an opaque identity-bound commit token, operation metadata, and safe fallback behavior for legacy handlers.
- [x] 4.2 Refactor `internal/executor` so every call follows one path: validate proposed arguments, run hard before-tool hooks, validate replacements, prepare the candidate, request permission, apply any approved revision through the same hard hooks and preparation again, route command tools through sandbox, execute, run post-tool hooks, and emit the result.
- [x] 4.3 Emit correlated proposed, prepared, permission, executing, and finished facts with only post-hook effective arguments and previews visible to observed consumers; never expose an obsolete pre-hook preview as authoritative.
- [x] 4.4 Implement create and modify candidate preparation for the existing built-in `write_file` and `edit_file` handlers, using unified hunks for decodable text and metadata-only previews for binary or undecodable content; keep delete as a generic preview operation for custom or future handlers without adding a Phase 7 delete tool.
- [x] 4.5 Bound prepared files/candidates and diff computation before allocation; bound the schema-v1 textual preview body at 64 KiB of valid UTF-8 or 800 logical preview lines, preserve truthful aggregate counts when computed, and emit a non-recoverable `preview_budget` omission for bounded or metadata-only fallback previews.
- [x] 4.6 Implement no-follow, directory-relative safe commit: capture and revalidate parent identity, target type/symlink state, stable target file ID, hard-link count, and preimage; reject existing `nlink != 1`; preserve ownership/mode/flags/ACL/xattrs through verified descriptor-based metadata cloning and atomic exchange; roll back the final identity race; preserve umask for new files and use atomic exclusive publication; fail closed when required primitives are unavailable or any component changed.
- [x] 4.7 Replace display-string-only executor truncation with valid-text bounded results plus structured `output_budget` omissions while retaining the current legacy truncation marker for old consumers.
- [x] 4.8 Add deterministic prepared-action tests for built-in create/modify, multi-hunk, empty, binary, invalid encoding, candidate/snapshot/diff/preview/output bounds, cancellation before atomic replacement, mode/owner/flags/ACL/xattr preservation, restrictive umask, final exchange rollback, same-byte inode replacement, hard-link aliases, symlink/parent/target races, unsupported identity primitive, hook replacement, and stale preimages, plus a fake custom handler proving generic delete preview without granting a built-in capability.

## 5. Harden permission revision, grants, and mode safety

- [x] 5.1 Give each rich permission request a stable request ID, preview revision, effective arguments, explanation, origin, optional remembered-rule scope, protocol/capability flags, and exactly-once reply operation returning typed `accepted`, `validation_rejected`, or `already_resolved` outcome; wrap custom-dispatcher legacy prompts as limited `legacy_one_shot` requests.
- [x] 5.2 Implement `revise_arguments` as a non-approving decision that returns typed feedback without closing the request when reply/schema validation fails; once accepted, close the request exactly once, rerun matching hard before-tool hooks, re-prepare the candidate, and emit a new request ID and preview revision that still requires allow or deny.
- [x] 5.3 Revalidate schema and rerun before-tool hooks for the legacy edit-and-allow reply path; execute unchanged human-edited arguments under the existing approval, but when hooks replace them issue a fresh legacy permission request so old consumers never authorize undisplayed effective arguments.
- [x] 5.4 Return typed feedback for malformed, schema-invalid, mismatched, or inapplicable rich replies without consuming the open request; resolve a schema-valid revision exactly once before hooks, terminate on hard-gate or preparation failure, reject stale/duplicate replies, and ensure no undisplayed preview revision can execute.
- [x] 5.5 Carry per-call sandbox grants as explicit ephemeral values through permission and sandbox routing, merge them only for the approved call, and keep them out of remembered permission rules.
- [x] 5.6 Keep bypass entry a frontend-confirmed action through public typed mode APIs; reject mode changes during a run and exclude bypass from the casual safe-mode cycle.
- [x] 5.7 Add race and table-driven tests for invalid-reply retry with the request still open, accepted revision, deny, allow, remember, duplicate reply, cancel, close, hook replacement/block with no replacement request, preparation failure with no replacement request, stale preview, ephemeral grants, legacy edit, and mid-run mode change.

## 6. Expose safe bootstrap, inventory, and delegated provenance

- [x] 6.1 Add public `LoadSettings` with the canonical home-then-project merge, defaults, validation, environment-reference, file-context error, and missing-file behavior while keeping internal configuration as the implementation owner.
- [x] 6.2 Add a public frontend-neutral bootstrap that validates and constructs the standard provider, hooks, permission engine, sandbox policy, built-in tools, and `Session` through the existing construction and execution paths.
- [x] 6.3 Use positive allowlists for public settings views, bootstrap diagnostics, descriptors, formatting, and serialization so credentials, environment values, system prompts, hook command arguments, and remembered-rule details cannot escape.
- [x] 6.4 Preserve the existing internal `Load`, `LoadFrom`, remembered-permission persistence, explicit SDK construction, and configuration merge behavior with compatibility tests.
- [x] 6.5 Build deterministic effective tool and hook inventory from the capabilities actually advertised and executable by the session; descriptor visibility must never add execution authority.
- [x] 6.6 Add optional capability reporters for external categories so skills or MCP can appear only when a real custom or future provider reports support and source; do not implement a skills runtime or MCP client in Phase 7.
- [x] 6.7 Give root and child agents stable opaque IDs and emit root-visible child start/finish provenance with parent, depth, delegation call, label, timestamps, and completed, failed, cancelled, or reached-step-limit outcome.
- [x] 6.8 Keep raw child text, reasoning, tool, hook, usage, warning, omission, error, and terminal streams isolated; report a depth-limit refusal as the task error without fabricating a child lifecycle.
- [x] 6.9 Add adversarial secret-hygiene, inventory-authority, caller-owned dispatcher permission/reply-path, duplicate-label, depth-limit, step-limit, cancellation, and raw-child-isolation tests.
- [x] 6.10 Seed public first-party `Bootstrap` with non-empty Coragent product framing that distinguishes product identity from the replaceable model backend, keep explicit `SessionConfig.SystemPrompt` authoritative for direct SDK construction, and prove both paths from captured offline provider input.

## 7. Establish the Bubble Tea application shell

- [x] 7.1 Recheck security and compatibility of the approved Charm v2 versions, then add Bubble Tea v2, Bubbles v2, Lip Gloss v2, and Glamour v2 dependencies without introducing a second terminal framework.
- [x] 7.2 Replace the placeholder `tui` package with a single-owner `AppModel`, orthogonal run/focus/scroll/mode/terminal state, injected clock, and a narrow `SessionPort` whose production adapter imports only `pkg/agent`.
- [x] 7.3 Implement non-blocking commands for startup, observed-event reads, permission replies, cancellation, session close, resize, and activity ticks; re-arm after every item, keep safe drain active after protocol rejection, and treat unknown v1 kind, invalid payload, or EOF-before-terminal as fatal bounded-shutdown conditions rather than wedging.
- [x] 7.4 Add a stable ordered transcript block store with indexes for call, request, agent, and run IDs, plus terminal fan-out that settles every active block/modal/animation from the authoritative run outcome and width-and-visual-mode render caching for completed blocks.
- [x] 7.5 Build the fixed shell regions: one-line project/mode/sandbox header, ordered transcript viewport, activity/model/context/unread footer, multiline composer, and contextual shortcut hints.
- [x] 7.6 Wire `cmd/coragent` through public settings/bootstrap into the `tui` adapter, handle startup failure before accepting input, restore the terminal on every exit path, and print close failures only to stderr.
- [x] 7.7 Add import-boundary tests proving `tui` and `cmd/coragent` do not import `internal/*` and harness packages do not import frontend packages.
- [x] 7.8 Replace manual composer-string editing with Bubbles v2 textarea and real terminal-caret integration, preserving position-aware multiline edits, focus visibility, logical cursor position across resize, and grapheme-safe Unicode behavior.

## 8. Render the ordered execution narrative

- [x] 8.1 Implement streaming assistant blocks that preserve event order, update only the active tail, remain pinned at the bottom until the user browses history, and show an unread count while browsing.
- [x] 8.2 Implement streaming-safe Markdown by caching a stable completed prefix through Glamour, progressively re-rendering only the bounded sanitized active tail so recognizable Markdown is styled from the first visible delta batch, keeping only the shortest ambiguous suffix literal, converging to a deterministic full-source render on completion, and covering first-delta heading/list/emphasis, split delimiters, open fences, table promotion, no-color/ASCII, and terminal-width bounds offline.
- [x] 8.3 Sanitize assistant text, reasoning summaries, tool arguments/results, diffs, paths, labels, errors, and pasted input before width measurement by stripping or visibly escaping HTML, images, OSC, OSC 52, CSI, unsafe hyperlinks, and disallowed C0 controls.
- [x] 8.4 Render provider-authored reasoning summaries as optional inline disclosures, never infer reasoning from assistant prose or elapsed time, and omit the component when unsupported or empty.
- [x] 8.5 Render one in-place tool row per call ID across proposed, prepared, awaiting permission, executing, succeeded, failed, cancelled, and hook-blocked states, with effective arguments and durations available on expansion.
- [x] 8.6 Render structured create/modify/delete previews and diffs with operation, path, revision, aggregate counts, bounded preview markers, stale-preview errors, and no frontend filesystem reads.
- [x] 8.7 Fold fully retained long results locally while keeping them expandable, render irreversible output, preview, provider-length, filter, redaction, and future-compaction omissions with reason-specific recovery copy, and let only an explicitly resumable cutoff prefill an editable continuation draft without auto-sending or overwriting existing text.
- [x] 8.8 Render hook outcomes and sandbox fallback as safety notices distinct from permission denials, and render subagents as indented lifecycle rows whose final answer remains the normal task tool result.
- [x] 8.9 Implement the on-demand inspector for safe model, mode, sandbox, context, tool, hook, and optional externally reported skills/MCP facts without displaying unsupported categories as loaded-zero claims.
- [x] 8.10 Add a one-cell non-color-dependent scrollbar and focus-independent mouse-wheel-only history scrolling through the semantic-anchor, clamp, pinning, and unread reducer path; preserve composer focus, draft, logical insertion point, and caret; keep Bubble Tea standard mouse tracking active because it has no wheel-only reporting mode; implement unmodified left-button drag as visible application-managed selection over a pane-clipped render snapshot and copy the selected text through OSC 52 on release; retain `XTSHIFTESCAPE n=0` plus terminal-native `Shift/Option+drag` only as a fallback; and leave scrollbar clicking and thumb dragging out of scope.

## 9. Implement focused safety interactions and shortcuts

- [x] 9.1 Build a modal permission sheet that traps focus and routes all keys before composer, mode, inspector, or global shortcuts while also suppressing background wheel scrolling; include action, reason, origin, preview revision, remembered scope, and sandbox grants.
- [x] 9.2 Support allow once, deny, remember, and edit actions through `submitting` state; keep the modal until `accepted`, restore drafts and clear the guard on `validation_rejected`, reconcile `already_resolved`, make `Esc` deny only, and keep preview scrolling or expansion inside the modal.
- [x] 9.3 Build the bounded JSON argument editor with inline syntax/schema errors; make `Ctrl+S` submit only a revision, retain the draft when public reply/schema validation leaves the request open, show re-preparing state after acceptance, and expose a new permission request only when hard hooks and preparation succeed.
- [x] 9.4 Implement Enter to submit, Ctrl+J as the baseline newline, sanitized bracketed multiline paste with PATH-resolved clipboard helpers disabled, and advertise modified-Enter aliases only when enhanced keyboard support is reported.
- [x] 9.5 Implement idle-only `Shift+Tab` cycling through default, auto-accept-edits, and plan; use `Ctrl+B` plus explicit confirmation for bypass, make `Shift+Tab` leave bypass for default, and show non-selectable `EXTERNAL`/`UNSUPPORTED` ownership states with both mode shortcuts disabled.
- [x] 9.6 Remove the `PageUp` transcript-focus path and every keyboard transcript-scroll binding so `PageUp`, `PageDown`, arrows, `j/k`, `Ctrl+U/D`, `End`, and `G` never move background history; preserve their normal composer or overlay semantics, implement `Ctrl+I` inspector and context-sensitive `Ctrl+/` help with modal precedence, and make help identify wheel-only history, direct drag copy, and terminal-native `Shift/Option+drag` as a fallback.
- [x] 9.7 Implement `Ctrl+C` cancellation and a four-second `Ctrl+Q` shutdown from every focus: deny an open parent request once, cancel, drain up to two seconds, close with a separate two-second context, then always restore the terminal and report forced nonzero shutdown if caller-owned work violates cancellation.
- [x] 9.8 Add reducer tests for every focus/key combination, paste, enhanced-key fallback, accepted/rejected/already-resolved allow/deny/remember/grant/revision replies, preserved drafts, duplicate keys, mode rejection, cancel, quit, close error, and exactly-once permission reply.

## 10. Apply Terminal Narrative responsively

- [x] 10.1 Encode the approved semantic color, spacing, border, glyph, and typography roles in one visual-token package, using the host terminal font and retaining labels plus structure in 256-color, no-color, and ASCII modes.
- [x] 10.2 Implement the single-cell execution marker and its proposed, active, permission, success, warning, error, cancelled, and hook-blocked glyph states, with inline state text and indented result branches instead of per-event cards or a fixed telemetry column.
- [x] 10.3 Implement 160x48 wide, 120x36 standard, 80x24 compact, 60x20 minimal, and below-60x20 too-small layouts using terminal-cell widths, whole-segment path compression, and priority-based chrome reduction.
- [x] 10.4 In the too-small view keep deny, cancel, quit, and resize guidance available but disable allow, remember, argument edit, and grant edit until at least 60x20; at supported sizes give modal previews their own viewport rather than allowing actions to scroll away.
- [x] 10.5 Implement context footer source labels and 80/95 percent semantic thresholds only when the window is known; show absolute `est` usage without a false percentage when it is unknown.
- [x] 10.6 Limit motion to the 120 ms active rail glyph and 600 ms cursor cadence, stop idle ticks, expose reduced-motion/static behavior, and keep animation from changing geometry.
- [x] 10.7 Verify CJK, combining marks, emoji, narrow/wide glyphs, long unbroken text, terminal resize, and palette downsampling using cell widths rather than byte or rune counts.

## 11. Verify the product and preserve the design contract

- [x] 11.1 Build deterministic observed-event fixtures covering empty, streaming, reasoning, tool preparation/execution, folded result, every omission, permission/revision/grants, hook block, sandbox fallback, subagent, cancellation, step limit, provider cutoff/filter, context thresholds, unknown v1 kind, invalid payload, EOF-before-terminal, startup error, and close error.
- [x] 11.2 Record the reviewer's Figma gate as one of: no waiver and coding waits, temporary implementation waiver with the editable-Figma task left open, or permanent waiver making the versioned SVG/PNG plus handoff the final design artifact.
- [x] 11.3 After Phase 0 approval and when Figma MCP capacity is available, populate the created Figma file through P1 to P4 with variables, components, eight content pages plus two dividers, responsive and safety frames, prototype links, screenshots, and developer handoff; leave this task open under no or temporary waiver, and close it only after the editable file is complete or the reviewer grants a permanent waiver.
- [x] 11.4 Add reducer transition snapshots and golden renders for 160x48, 120x36, 80x24, 60x20, and too-small permission layouts in truecolor, 256-color, no-color, reduced-motion, and ASCII modes.
- [x] 11.5 Add burst-stream and long-history benchmarks that assert bounded allocations, cached completed blocks, responsive input, and no full-transcript rerender on each text delta.
- [x] 11.6 Add PTY tests for resize, bracketed paste, enhanced keyboard detection, Ctrl+C, bounded Ctrl+Q with never-closing providers and blocking caller-owned work, terminal restoration after panic/error/forced shutdown, and control-sequence injection.
- [x] 11.7 Manually verify Ghostty, iTerm2, Terminal.app, tmux, VS Code terminal, JetBrains terminal, one Linux console, and one Windows terminal or documented equivalent fallback matrix.
- [x] 11.8 Run `gofmt`, `go test -count=1 ./...`, `go test -race ./...`, `go vet ./...`, `golangci-lint run ./...`, `go build ./cmd/coragent`, `git diff --check`, and `openspec validate phase-7-tui --strict` with workspace-local caches where required.
- [x] 11.9 Update public SDK documentation, TUI shortcut/help copy, settings/bootstrap examples, terminal support notes, accessibility/fallback notes, and Phase 7 acceptance evidence without changing prior-phase contracts.
- [x] 11.10 Reconcile every requirement and scenario in the Phase 7 delta specs with an automated test, golden, benchmark, manual-matrix entry, design-artifact evidence, or explicit source-compatibility check before declaring the phase complete.
- [x] 11.11 Add reducer, golden, and PTY regressions for real-caret focus and Unicode edits, non-overflow and overflow scrollbar states, wheel-only clamping, resize anchors, unread/live-bottom transitions, composer focus/draft/caret preservation during scrolling, always-on standard mouse tracking, direct drag selection and OSC 52 copy constrained to the current pane including wide graphemes and out-of-bounds releases, absent keyboard history routing, terminal-native copy fallback help, and first-party Coragent identity framing with explicit SDK prompt preservation.
